#include "main.h"

/**
 * _abs - prints absolute number
 * @f: parameter to be checked
 *
 * Return: Absolute value
 */

int _abs(int f)
{
	if (f >= 0)
	{
		return (f);
	}
	else
	{
		return (-f);
	}
}
