#include "main.h"

/**
 * _strlen - returns the length of a string
 * @s: string to be checked
 *
 * Return: number
 */
int _strlen(char *s)
{
	int len;

	for (len = 0; s[len]; ++len)
	{
		;
	}

	return (len);
}
